#!/usr/bin/env ruby

require './lib/console'
require 'ui'
require 'tictactoe'
require 'tictactoe_rules'
require 'game'

io = Console.new
ui = Ui.new(io)
ttt = TicTacToe.new
rules = TictactoeRules.new
new_game = Game.new({gametype: ttt, ui: ui, rules: rules})
new_game.play