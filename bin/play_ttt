#!/usr/bin/env ruby

require './lib/console'
require 'ui'
require 'tictactoe'
require 'game'

io = Console.new
ui = Ui.new(io)
ttt = TicTacToe.new
new_game = Game.new({gametype: ttt, ui: ui})
new_game.play